同学，T01组的任务2目前评审意见如下：

开发计划：
1.很好很全面

需求文档：
1.用例说明与用例图略有不一致。整体不错，唯一瑕疵在于，用例说明格式中缺少了“主动参与者/被动参与者”一项，所以对于“登录”和“管理旅游方案”这些用例，在管理员和导游之间应拆分还是统一是有些混淆的。对于“登录”用例，配合你们的UI设计，无论管理员还是导游都会使用同一个界面，只不过要点选不同身份类型，所以作为其用例说明，应该标出主动参与者有管理员和导游两类，且只需在基本流中加一条“若用户选择管理员身份，则执行管理员登录子流……”，然后在基本流中额外增加一条相应的子流说明即可。对于“管理旅游方案”之类的用例，管理员和导游的界面应有比较大的差别，即管理员可以查看所有导游的所有方案，但导游只能查看自己的方案，所以是否使用同一个UI来操作，进而是否用一个用例还是分开为两个用例，就看你们的设计规划了。如果归为一个用例和UI的话，用例说明写法可与“登录”用例用子流表达类似。如果分为两个用例来说明，则用例图中也要画成两个不同命名的用例，不可用一个椭圆做共同代表。

用户原型：
1.界面之间的导航路径没有明确定义

目前得分见 项目考核评估表，低于9.8分的可以考虑做相应修改（再次提交会扣0.2分），请通知你们组相应同学最晚于周三（12日）进行二次提交。
